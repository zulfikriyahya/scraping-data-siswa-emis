EMIS_EMAIL=
EMIS_PASSWORD=

# Opsional — isi untuk skip auto-detect (hemat request, hindari WAF).
# Kosongkan (hapus baris/comment) untuk deteksi otomatis ulang.
TOTAL_PAGES_OVERRIDE=33
LAST_PAGE_COUNT_OVERRIDE=9

SCRAPER_WORKERS=1
ROWS_PER_PAGE=30

NAV_TIMEOUT_MS=45000
SELECTOR_TIMEOUT_MS=20000
DETAIL_WAIT_MS=1000
RETRY_WAIT_MS=1500
POST_BACK_WAIT_MS=500
PROBE_TIMEOUT_MS=10000
PROBE_RETRIES=1
WARMUP_TIMEOUT_MS=60000

AUTH_FILE=auth.json
BACKUP_FILE=progress_backup.json
OUTPUT_FILE=data_siswa_emis.xlsx
BASE_URL=https://emis.kemenag.go.id/kesiswaan
